# Use Phaser with Matter Physics

Dunia Zee will use Vite, TypeScript, and Phaser 4 with its bundled Matter Physics integration. The earlier PixiJS direction would require us to connect rendering, input, scene state, and a separate physics engine ourselves, while Phaser provides those game systems together and still produces a static browser application. Physics will remain two-dimensional beneath the 2.5D artwork.
